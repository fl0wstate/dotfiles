#include <stdio.h>
#include "functions.h"

/**
 * main - simple add Function
 * Return: a positive number
 */
int main(void)
{
        int a = 5, b = 10;
        printf("Sum: %d\n", add(a, b));
        return 0;
}
